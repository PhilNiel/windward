# windward
A weather data platform on k3s: uses the Open Meteo API to ingest data through a medallion pipeline (bronze/silver/gold), orchestrated with Airflow, transformed with dbt, and provisioned end to end with Terraform.
